<h1>Matches</h1>
<table>
  <th>Student</th>
  <th>Employer</th>
  <th>Create Interview</th>

  <% @matches.each do |m| %>  
    <tr class="<%= cycle('list_line_odd', 'list_line_even') %>">
      <td> <%= link_to m[0], student_path(m[2])  %> </td> 
      <td> <%= link_to m[1], employer_path(m[3]) %></td>
      <td> <%=  %> </td>
    </tr>
  <% end %>
</table>