<h1>Employers List</h1>
<%= link_to 'Add New Employer', new_employer_path %>
<table id="employers_list">
  <th>Name</th>
  <th>Industry</th>
  <th>Location</th>
  <th>Year Established</th>
  <th>Description</th>
  <th>Funding</th>
  <th># Employees</th>
  <% @employers.sort.each do |e| %>
      <tr class="<%= cycle('list_line_odd', 'list_line_even') %>">
        
        <td>
        <%= link_to e.name, employer_path(e.id) %> <br>
        </td>
        <td><%= e.industry %>
        </td>
        <td><%= e.location %>
        </td>
        <td><%= e.est_year %>
        </td>
        <td><%= e.description %>
        </td>
        <td><%= e.funding %>
        </td>
        <td><%= e.number_of_employees %>
        </td>
      </tr>
  <% end%>
  
</table>

<br />

<%= link_to 'Add New Employer', new_employer_path %>